<%= error_messages_for @user %>

<%= form_for(@user, :action => resource(@user)) do %>
  <p>
    <%= text_field :login, :label => "Login" %>
  </p>
  <p>
    <%= submit "update" %>
  </p>
<% end =%>

<%= link_to 'Show', resource(@user) %> | <%= link_to 'Back', resource(:users) %>
